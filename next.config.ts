/*
 * Fil: next.config.ts
 * Syfte: Konfiguration för Next.js-projektet.
 * Vad koden gör: Styr globala build-/runtime-inställningar för appen.
 * Lär dig: Denna fil ändras när du behöver särskilda Next.js-inställningar.
 * Felsökning: Vid buildproblem eller framework-inställningar, kontrollera här.
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
